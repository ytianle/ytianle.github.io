# yuantianle.github.io

mkdocs serve

mkdocs serve --dirtyreload

mkdocs build

Rule:
- Any page comes with a folder. And in the folder, the page is named README.md
- Please turn on the drag-file mode to sync new file creation or deletion.
- Please turn off the drag-file mode (drag-nav-only) mode to arrange existing files in the nav.